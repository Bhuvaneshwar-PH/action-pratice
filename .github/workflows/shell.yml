name: Executing Shell script

on: 
    push: 
        branches: ['main']

jobs:
    shell-execute:
        runs-on: ubuntu-latest
        steps:
            - name: checout repo
              uses: actions/checkout@v4
            - name: execute shell
              run: |
                chmod +x test.sh
                ./test.sh